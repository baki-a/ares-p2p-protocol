name: Autograding Tests
'on':
- push
- repository_dispatch
permissions:
  checks: write
  actions: read
  contents: read
jobs:
  run-autograding-tests:
    runs-on: ubuntu-latest
    if: github.actor != 'github-classroom[bot]'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Junit testing
      id: junit-testing
      uses: classroom-resources/autograding-command-grader@v1
      with:
        test-name: Junit testing
        setup-command: ''
        command: mvn test
        timeout: 10
    - name: Autograding Reporter
      uses: classroom-resources/autograding-grading-reporter@v1
      env:
        JUNIT-TESTING_RESULTS: "${{steps.junit-testing.outputs.result}}"
      with:
        runners: junit-testing
